<LinearLayout

    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="@dimen/widget_margin"
    android:background="@color/colorPrimaryLight"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/widget_image"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:contentDescription="@string/app_icon_image"
        android:src="@drawable/launch_icon"
        android:layout_margin="@dimen/standard_padding_margin"
        android:layout_gravity="center">
    </ImageView>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/standard_padding_margin"
        android:orientation="vertical"
        android:layout_weight="2"
        android:layout_gravity="center">

        <TextView
            android:id="@+id/appwidget_text_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/appwidget_text"
            android:text="Request for Transport"
            android:textAppearance="@style/TextAppearance.AppCompat.Menu"
            android:layout_gravity="center"/>

        <TextView
            android:id="@+id/appwidget_text_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_margin="@dimen/widget_margin"
            android:textAppearance="@style/TextAppearance.AppCompat.Body2"
            tools:text="Aug. 12, 2019">
        </TextView>

        <TextView
            android:id="@+id/appwidget_text_destination"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:textAppearance="@style/TextAppearance.AppCompat.Body1"
            tools:text="Modesto to Sacramento">
        </TextView>
    </LinearLayout>
</LinearLayout>